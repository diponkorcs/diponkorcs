# My Portfolio
 
# run with "npm run dev"

# some packages have already been deprecated, please check later

# Upload in  Netlify by using "npm run build" -> a "Dist" folder will be created -> Convert that Dist folder into a ZIP file -> Upload